import { Bulletin, PagasaParserSource } from "pagasa-parser";

export default class PagasaParserTemplate extends PagasaParserSource {

    async parse(): Promise<Bulletin> {
        // Do things with constructor data.
    }

}
